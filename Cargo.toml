[package]
name = "hello"
version = "0.0.0"
edition = "2021"

[lib]
path = "api/hello/message.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
adaptivemsg = { path = "../adaptivemsg-rust" }

# TODO: set a real version and adaptivemsg dependency before publishing.
